# Copy this file to .env and fill in the values for your setup.
# docker-compose.yaml reads .env automatically for variable substitution.

# ── Frontend URLs (baked into the CRA build via Docker build args) ─────────────
# REACT_APP_API_BASE=http://localhost:8000
# REACT_APP_BACKEND_URL=http://localhost:5001

# ── AI Provider ────────────────────────────────────────────────────────────────
# Choose one profile below and uncomment it.

# Profile A — Ollama Cloud (dev-friendly, no local Ollama required)
#   API key: https://ollama.com/settings/keys   Models: https://ollama.com/library
AI_PROVIDER=ollama_cloud
OLLAMA_API_KEY=your-ollama-cloud-api-key
MODEL_NAME=gemma3:4b
# Node backend also needs an OpenAI-compatible key (same value as OLLAMA_API_KEY)
LLM=openaiService
LLM_URL=https://ollama.com/v1/chat/completions
OPENAI_API_KEY=your-ollama-cloud-api-key

# Profile B — Local Ollama (full privacy, requires `ollama serve` on the host)
# AI_PROVIDER=ollama
# OLLAMA_BASE_URL=http://host.docker.internal:11434
# MODEL_NAME=gemma3:1b
# LLM=ollamaService
# LLM_URL=http://host.docker.internal:11434/api/generate

# Profile C — OpenAI-compatible endpoint
# AI_PROVIDER=openai
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_API_KEY=your-openai-api-key
# MODEL_NAME=gpt-4o-mini
# LLM=openaiService
# LLM_URL=https://api.openai.com/v1/chat/completions
